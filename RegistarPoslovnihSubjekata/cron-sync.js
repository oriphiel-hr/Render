#!/usr/bin/env node
/**
 * Cron script: sync endpointi uklonjeni – skripta više ne šalje zahtjeve.
 * Za Render Cron Job može ostati (izlaz 0) ili se ukloniti iz konfiguracije.
 */
console.log('Sync is disabled (no endpoints).');
process.exit(0);
