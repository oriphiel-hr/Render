# Lokalno: kopiraj u .env i popuni vrijednosti.
# Na Renderu: Dashboard → Web Service → Environment (vrijednosti su tamo).

# Baza (Prisma) – obavezno za expected_counts i sync
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Sudski registar API – OAuth (https://sudreg-data.gov.hr)
SUDREG_TOKEN_URL="https://sudreg-data.gov.hr/api/oauth/token"
SUDREG_CLIENT_ID=""
SUDREG_CLIENT_SECRET=""

# Opcionalno: base URL Sudreg API-ja (default: https://sudreg-data.gov.hr/api/javni)
# SUDREG_API_BASE="https://sudreg-data.gov.hr/api/javni"

# Zaštita upisa u bazu – obavezno za POST /api/sudreg_expected_counts (i budući sync).
# Generiraj dugi nasumični string; šalje se u headeru X-API-Key ili Authorization: Bearer <key>
SUDREG_WRITE_API_KEY=""

# PORT – Render postavlja automatski; lokalno default 3000
# PORT=3000
